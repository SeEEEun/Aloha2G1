{
  "schema_version": "equal_xr_post_training_shared_v1",
  "status": "DISABLED_PENDING_FINAL_CAMERA_XR_DATA",
  "execution_allowed": false,
  "dataset": "DATASET_NOT_COLLECTED",
  "adaptation_split_manifest": "DATASET_NOT_COLLECTED",
  "camera_config": "configs/camera_presets/HELMET_D455_FINAL_PENDING.json",
  "required_camera_status": "FROZEN_HELMET_D455_FINAL",
  "required_camera_serial": "CAMERA_NOT_MOUNTED",
  "required_camera_calibration_sha256": "EXTRINSIC_NOT_CALIBRATED",
  "task": "Pick up the doll with the left hand, handoff it to the right hand, and place it in the trash bin.",
  "input_camera_key": "observation.images.cam_high",
  "state_dimension": 28,
  "action_dimension": 28,
  "action_chunk_size": 50,
  "normalization": "MEAN_STD from each source checkpoint plus the identical XR adaptation dataset",
  "steps": 1500,
  "batch_size": 16,
  "optimizer": "AdamW",
  "peak_learning_rate": 0.00001,
  "final_learning_rate": 0.000001,
  "warmup_steps": 50,
  "save_every_steps": 500,
  "seed": 20260825,
  "visual_adaptation_strategy": "identical paired final-camera XR adaptation with the same trainable subset",
  "checkpoint_selection_rule": "select once from steps 500/1000/1500 by the same frozen 9-phase offline aggregate; ties select the earliest step",
  "fairness_invariants": [
    "exact same XR episode subset and split-manifest hash",
    "exact same optimization-step and sample budget",
    "exact same camera-calibration hash",
    "exact same task, 28D named schema, normalization principle and chunk length",
    "exact same offline probe and closed-loop rollout evaluator"
  ],
  "real_robot_commands": false
}
